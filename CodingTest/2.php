<?php
	// Parameter words: string
	// Return string maximum palindrome
	set_time_limit(5);
	if (isset($_GET['words'])) {
		$chars = str_split(str_replace(' ', '', $_GET['words']));
		$vals = [];
		for ($i = 1; $i < count($vars) / 2; $i++) {
			for ($j = 0; $j < count($vars) - $i + 1; $j++) {
				$tmp = [];
				for ($)
			}
		}
	}
